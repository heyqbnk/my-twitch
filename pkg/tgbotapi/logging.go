package tgbotapi

// Logs error in case, logger contains specified function.
func (b *Bot) logError(err error) {
	// if b.logger == nil || b.logger.Error == nil {
	// 	return
	// }
	// b.logger.Error(err)
}
